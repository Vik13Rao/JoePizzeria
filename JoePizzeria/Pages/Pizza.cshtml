@page
@model JoePizzeria.Pages.PizzaModel
@{
}

<div class="text-center">
    <div class="mainEffect">
        <h1 class="display-4 text-capitalize text-black">
            List of pizzas</h1>
        
    </div>
    <div class="container">
        <div class="row">
            @*<div class="col-12 mb-5 mt-5">
                <div class="w-100 row makeDiv">
                    <div class="col-4">
                        <img src="~/images/Pizzas/custom.png" class="w-100" />
                    </div>
                    <div  class="col-8 align-items-center">
                        <div class="align-middle makeText">
                            <h3 style="text-align:left;">Create Pizza</h3>
                            <a class="btn btn-primary" style="float: left" asp-area="" asp-page="Forms/CustomPizza">Try now</a>
                        </div>
                    </div>
                </div>
            </div>*@
            @foreach (var pizzaItem in Model.mockPizzaDB)
            {
                var imagePath = "~/images/Pizzas/" + (pizzaItem.ImageUrl + ".png");
                var namePath = pizzaItem.PizzaName;
                <div class="col-4 mb-5">

                    <img src="@imagePath" class="img-fluid" asp-append-version="true" />
                    <p>@namePath</p>
                    <a class="btn btn-primary mt-1" asp-area="" asp-page="Checkout/Checkout" 
                    asp-route-ImageUrl="@pizzaItem.ImageUrl" asp-route-PizzaPrice="@pizzaItem.FinalPrice"
                   asp-route-PizzaName="@pizzaItem.PizzaName" asp-route-OrderId="@pizzaItem.OrderId">Buy Now</a>
                    <br />


                </div>

            }
            
        </div>

    </div>
</div>